# Dependencias solo para desarrollo y tests (no las necesita Render para arrancar la API).
# Instalar en local con:  pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.3
requests==2.32.3
httpx==0.27.2